# 第一階段
#FROM golang:1.18.1-alpine AS builder
FROM golang:1.23-alpine AS builder

RUN apk update && apk add --no-cache git

WORKDIR /app

#ENV GOPROXY=https://goproxy.io // go mod download 失敗請把註解拿掉

COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN go build -o blackbox_agent

# 第二階段
FROM alpine:latest

RUN apk --no-cache add ca-certificates tzdata
ENV PATH="/venv/bin:$PATH" \
    TZ="Asia/Taipei"


WORKDIR /root/

COPY --from=builder /app/blackbox_agent .
COPY --from=builder /app/yaml ./yaml
COPY --from=builder /app/yaml/confignew.yaml ./yaml/config.yaml
COPY --from=builder /app/pkg/blackbox_exporter ./blackbox_exporter
#COPY --from=builder /app/prometheus.crt ./prometheus.crt

EXPOSE 8080

CMD ["./blackbox_agent"]
